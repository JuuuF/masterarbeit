
% UTF-8 Encoding
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}     % Use T1 font encoding

% SPRICH DEUTSCH, DU *********
\usepackage[ngerman]{babel}

% Worttrennungen verringern
\tolerance=500
\hyphenpenalty=1000

% citation
\usepackage[numbers]{natbib}

% Graphics
\usepackage{graphicx}

% SVG
\usepackage{svg}
\svgpath{{imgs/}}

% Maths
\usepackage{amsmath}

% Hyperlinks
\usepackage{hyperref}
\hypersetup{
    colorlinks=true,
    linkcolor=gray!80!black,
    urlcolor=blue,
    citecolor=green!50!black,
}
\usepackage{cleveref}

% Gantt charts
\usepackage{pgfgantt}

% Correct quotation marks
\usepackage{csquotes}
\MakeOuterQuote{"}

% Custom Colors
\usepackage{xcolor}

% TODO notes
\newcommand\todo[1]{\textbf{\textcolor{red}{[#1]}}}


% -----------------------------------------------
% commands

\renewcommand{\quote}[1]{\glqq#1\grqq\,}

% -----------------------------------------------

% \newcommand{\bitTitle}{Entwicklung eines KI-basierten Single-Camera-Systems für das Scoring von Steeldarts durch die synthetische Erzeugung realistischer Daten}
\newcommand{\bitTitle}{Nutzung von realitätsnahen, synthetisch erzeugten Daten zur Verbesserung des KI-gestützten Scorings von Steeldarts in einem Single-Camera-System}
\newcommand{\bitAuthor}{Justin Fürstenwerth}
\newcommand{\bitEmail}{stu{\bf 210298}@mail.uni-kiel.de}

% -----------------------------------------------
% light / dark mode switch

\usepackage{ifthen} % For conditional logic

% Define the mode variable
\newcommand{\mode}{light}
% \renewcommand{\mode}{dark}

% Set up colors based on the mode
\ifthenelse{\equal{\mode}{dark}}{
    \definecolor{background}{RGB}{31,31,31}
    \definecolor{foreground}{RGB}{204,204,204}
    \pagecolor{background}
    \color{foreground}
}{}
